const getTheTitles = function(array) {
    return array.map((book) => book.title,[]);
};

/*
Thoughts and Reflections

ABSOLUTELY NAILED IT. My solution was the exact same as the provided solution! I tried it with reduce at first but it didn't seem to be working but then it occured to me that I'm just kind of... transcribing, is the best way I can put it, the array so map should work just fine. 
*/

// Do not edit below this line
module.exports = getTheTitles;
